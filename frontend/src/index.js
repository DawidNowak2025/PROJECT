// This code is used to import React and the main application component.
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./App.css";

// This code is used to render the application inside the root div.
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
